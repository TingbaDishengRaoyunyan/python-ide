version: "3.9"
services:
  gpu-runtime:
    build: {context: ./runtimes/gpu}
    profiles: [gpu]
    gpus: all
    restart: unless-stopped
    command: ["python3", "-c", "import torch; print({'cuda': torch.cuda.is_available(), 'torch': torch.__version__})"]
