ADMIN_USERNAME=Yun_Yan+baili20130209
ADMIN_USERNAME_AUTO_PROMOTE=false
SESSION_SECRET=replace-with-output-of-openssl-rand-hex-32
DATA_ROOT=/data

# Production deployment:
# 1. Set a strong SESSION_SECRET and register the admin account through the private network.
# 2. Change deploy/Caddyfile from ide.example.com to your domain.
# 3. Point DNS A/AAAA records to this server.
# 4. docker compose -f docker-compose.prod.yml --env-file .env up -d --build
